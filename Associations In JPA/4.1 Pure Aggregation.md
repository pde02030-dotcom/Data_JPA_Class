

# 🟨 **📌 순수 Aggregation(Pure Aggregation)

# 🔷 **샘플 코드**

## 🟦 `Item` — 독립 Aggregate Root (Aggregation의 대상)

```java
@Entity
public class Item {

    @Id @GeneratedValue
    private Long id;

    private String name;
    private int price;
    private int stockQuantity;

    protected Item() {}

    public Item(String name, int price, int stockQuantity) {
        this.name = name;
        this.price = price;
        this.stockQuantity = stockQuantity;
    }

    public void removeStock(int qty) {
        if (this.stockQuantity < qty) {
            throw new IllegalArgumentException("재고 부족");
        }
        this.stockQuantity -= qty;
    }

    public void addStock(int qty) {
        this.stockQuantity += qty;
    }

    public String getName() {
        return name;
    }
}
```

---

## 🟦 `OrderItem` — Item을 “참조”만 하는 Aggregation 관계

```java
@Entity
public class OrderItem {

    @Id @GeneratedValue
    private Long id;

    @ManyToOne(fetch = FetchType.LAZY)   // 🔥 순수 Aggregation
    @JoinColumn(name = "item_id", nullable = false)
    private Item item;                   // 🔥 독립 엔티티 참조

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "order_id", nullable = false)
    private Order order;                 // Strong Composition (OrderItem → Order)

    private int orderPrice;
    private int quantity;

    protected OrderItem() {}

    public OrderItem(Item item, Order order, int price, int qty) {
        this.item = item;
        this.order = order;
        this.orderPrice = price;
        this.quantity = qty;
    }

    public int getTotalPrice() {
        return orderPrice * quantity;
    }

    public Item getItem() {
        return item;
    }
}
```
## 🟦 `Order` — OrderItem을 “참조”만 하는 Aggregation 관계
```
@Entity
public class Order {
    @Id @GeneratedValue
    private Long id;

    @OneToMany(mappedby = "order", orpah

}
```

---

# 🟨 **1. 순수 Aggregation이란 무엇인가? (샘플 코드 기반 정의)**

> **OrderItem이 Item을 참조하지만,
> Item은 OrderItem과 전혀 생명주기를 공유하지 않는 관계.**

즉:

✔ Item은 OrderItem 없어도 존재함
✔ OrderItem이 삭제돼도 Item은 삭제되면 안 됨
✔ 하나의 Item을 여러 OrderItem이 공유 가능
✔ OrderItem이 Item을 “소유(Composition)”하는 것이 아님
✔ OrderItem은 Item의 생명/죽음에 영향을 주지 않음
✔ Item은 독립 Aggregate Root

이 모든 특성이 샘플 코드에 정확히 드러난다:

```java
@ManyToOne(fetch = LAZY)  // Cascade 없음 → 생명주기 독립
private Item item;
```

---

# 🟨 **2. 왜 OrderItem → Item 관계는 순수 Aggregation인가?**

Item은 쇼핑몰에서 “상품”이며,
주문(OrderItem)이 없어도
상품(Item)은 이미 존재하고, 계속 존재해야 한다.

### ✔ Item은 독립 엔티티다

```java
Item item = new Item("노트북", 1500000, 10);
em.persist(item);
```

OrderItem 없이 그냥 저장됨.
**이게 Aggregation의 첫 번째 조건.**

---

### ✔ OrderItem은 Item의 생명주기를 조작할 수 없다

OrderItem이 삭제되어도 Item은 그대로 남아야 한다.

따라서 CascadeType.REMOVE / ALL 은 절대 금지.

---

### ✔ Item은 여러 OrderItem이 참조 가능(Shared Resource)

```
OrderItem #1 → Item A  
OrderItem #2 → Item A  
OrderItem #3 → Item A
```

Item 하나가 여러 OrderItem에서 쓰인다.
**이것이 Aggregation의 결정적 증거.**

---

# 🟨 **3. JPA 매핑 규칙 (샘플 코드 기반)**

## ✔ 1) Cascade 금지

```java
@ManyToOne(fetch = LAZY)
private Item item;
```

여기서 `cascade = CascadeType.ALL` 을 붙이면?

🚨 주문 취소(OrderItem 삭제) → Item 삭제됨 → 대참사
(실무에서 실제로 자주 일어나는 사고)

**Aggregation은 생명주기 분리를 해야 하는 관계이므로 Cascade 금지**

---

## ✔ 2) orphanRemoval 금지

orphanRemoval = true 는 “부모가 자식을 삭제하는 구조”
→ Composition 개념
→ Aggregation이 아님.

---

## ✔ 3) FK는 OrderItem에 존재하지만 의미는 “참조”일 뿐

마치 아래와 같은 DB 구조:

```
order_item
 ├─ id
 ├─ order_id  FK (Order)
 └─ item_id   FK (Item)
```

item_id가 FK라 해서
OrderItem이 Item을 소유한다는 의미가 아니다.
그저 참조일 뿐이다.

---

## ✔ 4) 저장/삭제는 항상 별도로 수행

```java
em.persist(item);        // Item 독립 저장
em.persist(orderItem);   // OrderItem 별도 저장
```

OrderItem.persist() → Item.persist() 전파 없음
OrderItem.remove() → Item.remove() 전파 없음

🔥 이것이 Aggregation.

---

# 🟨 **4. Hibernate 내부 동작 (샘플 코드 기반 전문 설명)**

### ✔ OrderItem.persist()

* Item은 영속성 컨텍스트에 전혀 변화 없음
* ActionQueue에 Item 관련 Action이 생성되지 않음

### ✔ OrderItem.remove()

* Item 관련 Delete Action 없음
* Item은 절대 영향을 받지 않음

즉,

> **OrderItem → Item 관계는 Hibernate ActionQueue에
> Item 관련 Action(INSERT/DELETE)을 생성하지 않는다.**

→ 생명주기 완전 분리 = Aggregation

---

# 🟨 **5. DDD Aggregate 관점 (샘플 코드 기반)**

### 구조는 이렇게 된다:

```
Order (Aggregate Root)
 └─ OrderItem (Order 내부 엔티티 — Composition)

Item (독립 Aggregate Root)
```

### 관계 해석

* OrderItem은 Order 내부 엔티티 → Strong Composition
* OrderItem → Item 은 다른 Aggregate Root 참조 → Aggregation

즉,
샘플 코드 하나로 **Composition + Aggregation**이 모두 표현되고 있다.

---

# 🟨 **6. 실무에서 발생하는 문제 (샘플 코드 기반)**

## ❌ CascadeType.ALL 실수

```java
@ManyToOne(cascade = CascadeType.ALL)
```

→ 주문 취소 → OrderItem 삭제 → Item 삭제
→ 다른 주문의 Item도 삭제 → 장애 발생

(실무에서 자주 실제로 일어남)

---

## ❌ OneToMany 단방향으로 잘못 설계

Aggregation은 ManyToOne 단방향이 정석
(샘플 코드가 정확한 정답)

---

## ❌ Lazy 로딩에 따른 N+1 문제

해결: fetch join, batch-size

---

# 🟩 **7. 최종 요약 — 샘플 코드 기반 Aggregation 완전 정의**

### ✔ OrderItem → Item 은 순수 Aggregation이다

* 두 엔티티는 생명주기 독립
* Cascade 없음
* orphanRemoval 없음
* Item은 독립 Aggregate Root
* 여러 OrderItem에서 공유 가능
* 저장/삭제 모두 분리
* 단순 참조 관계

### ✔ 코드로 증명됨

```java
@ManyToOne(fetch = LAZY)  
@JoinColumn(name = "item_id")
private Item item;
```

### ✔ OrderItem → Order 은 Strong Composition이다

(생명주기 공유)
따라서 두 관계는 완전히 다르다.


